PrimitiveArrayTools.{{slice_method}}({{kt_param_name}})
{%- if let Some(borrowed_lts) = borrowed_lts -%}
    .into(listOf({% for lt in borrowed_lts -%}
        {% if is_param %}{{ lifetimes.fmt_lifetime(*lt) }}Edges{% else %}*{{ lifetimes.fmt_lifetime(*lt) }}AppendArray{% endif %}
    {%- if !loop.last %}, {% endif %}{% endfor %}))
{%- endif -%}
