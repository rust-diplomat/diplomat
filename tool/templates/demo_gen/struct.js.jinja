(function (...args) {
    return {{type_name}}.FromFields({
        {%- for field in fields %}
        {{field}}: args[{{loop.index0}}]{% if !loop.last %},{% endif %}
        {%- endfor -%}
    });
})