inline {{m.return_ty}} {{m.type_name}}::{{m.method_name}}({% for param in m.param_decls %}
	{%- if !loop.first %}, {% endif -%}
	{{ param.type_name }} {{ param.var_name }}
{%- endfor %}){% for qualifier in m.post_qualifiers %} {{qualifier}}{% endfor %} {
	{{m.writeable_prefix}}{{m.return_prefix}}{{m.c_method_name}}({% for param in m.cpp_to_c_params %}
		{%- if !loop.first %},
		{% endif -%}
		{{ param }}
	{%- endfor %});{{m.return_statement}}
}
