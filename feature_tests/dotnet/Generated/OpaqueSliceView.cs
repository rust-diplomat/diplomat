// <auto-generated/> by Diplomat

using System;
using Somelib.Diplomat;

namespace Somelib;

#nullable enable

public partial class OpaqueSliceView : IDisposable
{
    private unsafe RustHandle<Raw.OpaqueSliceView>? _inner;

    private static readonly unsafe RustDestructor<Raw.OpaqueSliceView> _destroy = Raw.OpaqueSliceView.Destroy;

    /// <summary>
    /// Creates a managed <c>OpaqueSliceView</c> from a raw handle.
    /// </summary>
    /// <remarks>
    /// Safety: you should not build two managed objects using the same raw handle (may cause use-after-free and double-free).
    /// <br/>
    /// This constructor assumes the raw struct is allocated on Rust side.
    /// If implemented, the custom Drop implementation on Rust side WILL run on destruction.
    /// </remarks>
    internal unsafe OpaqueSliceView(Raw.OpaqueSliceView* handle)
    {
        _inner = RustHandle<Raw.OpaqueSliceView>.Owned(handle, _destroy);
    }

    /// <summary>
    /// Owned construction with lifetime edges released after the Rust
    /// destructor.
    /// </summary>
    internal unsafe OpaqueSliceView(Raw.OpaqueSliceView* handle, params ILifetimeEdge?[] edges)
    {
        _inner = RustHandle<Raw.OpaqueSliceView>.Owned(handle, _destroy, edges);
    }

    internal unsafe OpaqueSliceView(
        Raw.OpaqueSliceView* handle,
        WrapperKind kind,
        params ILifetimeEdge?[] edges)
    {
        _inner = RustHandle<Raw.OpaqueSliceView>.Borrowed(handle, kind, edges);
    }

    internal unsafe RustHandle<Raw.OpaqueSliceView> Handle
    {
        get
        {
            RustHandle<Raw.OpaqueSliceView>? inner = _inner;
            if (inner is null || inner.IsClosed)
            {
                throw new ObjectDisposedException("OpaqueSliceView");
            }
            return inner;
        }
    }

    /// <exception cref="SliceParseErrorException"></exception>
    /// <returns>
    /// A <c>OpaqueSliceView</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// <br/>
    /// Warning: this call pins the buffer passed via <c>ReadOnlyMemory</c> for as long as the returned value, and anything borrowing from it, is alive.
    /// The pin is released by <c>Dispose()</c> when the type has one, otherwise only when the garbage collector finalizes the value.
    /// Do not mutate the buffer while the returned value is in use, and keep the returned value short-lived if the buffer is large.
    /// </remarks>
    public static OpaqueSliceView Parse(ReadOnlyMemory<byte> data)
    {
        unsafe
        {
            DiplomatPinnedMemory? dataPin = null;
            try
            {
                dataPin = DiplomatPinnedMemory.Pin(data);
                var result = Raw.OpaqueSliceView.Parse(new DiplomatSliceU8 { Ptr = (byte*)dataPin.Pointer, Len = (nuint)data.Length });
                if (!result.IsOk)
                {
                    throw new SliceParseErrorException(new SliceParseError(result.Err));
                }
                return new OpaqueSliceView(result.Ok, dataPin);
            }
            catch
            {
                dataPin?.Dispose();
                throw;
            }
        }
    }

    /// <exception cref="SliceParseErrorException"></exception>
    /// <returns>
    /// A <c>OpaqueSliceView</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// <br/>
    /// Warning: this call pins the buffer passed via <c>ReadOnlyMemory</c> for as long as the returned value, and anything borrowing from it, is alive.
    /// The pin is released by <c>Dispose()</c> when the type has one, otherwise only when the garbage collector finalizes the value.
    /// Do not mutate the buffer while the returned value is in use, and keep the returned value short-lived if the buffer is large.
    /// </remarks>
    public static OpaqueSliceView ParseStrict(ReadOnlyMemory<byte> data)
    {
        unsafe
        {
            DiplomatPinnedMemory? dataPin = null;
            try
            {
                dataPin = DiplomatPinnedMemory.Pin(data);
                var result = Raw.OpaqueSliceView.ParseStrict(new DiplomatSliceU8 { Ptr = (byte*)dataPin.Pointer, Len = (nuint)data.Length });
                if (!result.IsOk)
                {
                    throw new SliceParseErrorException(new SliceParseError(result.Err));
                }
                return new OpaqueSliceView(result.Ok, dataPin);
            }
            catch
            {
                dataPin?.Dispose();
                throw;
            }
        }
    }

    /// <returns>
    /// A <c>OpaqueSliceView</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// <br/>
    /// Warning: this call pins the buffer passed via <c>ReadOnlyMemory</c> for as long as the returned value, and anything borrowing from it, is alive.
    /// The pin is released by <c>Dispose()</c> when the type has one, otherwise only when the garbage collector finalizes the value.
    /// Do not mutate the buffer while the returned value is in use, and keep the returned value short-lived if the buffer is large.
    /// </remarks>
    public static OpaqueSliceView Wrap(ReadOnlyMemory<byte> data)
    {
        unsafe
        {
            DiplomatPinnedMemory? dataPin = null;
            try
            {
                dataPin = DiplomatPinnedMemory.Pin(data);
                Raw.OpaqueSliceView* result = Raw.OpaqueSliceView.Wrap(new DiplomatSliceU8 { Ptr = (byte*)dataPin.Pointer, Len = (nuint)data.Length });
                return new OpaqueSliceView(result, dataPin);
            }
            catch
            {
                dataPin?.Dispose();
                throw;
            }
        }
    }

    public uint Length()
    {
        unsafe
        {
            BorrowLease<Raw.OpaqueSliceView>? selfLease = null;
            try
            {
                selfLease = Handle.Lease(BorrowKind.Shared);
                return Raw.OpaqueSliceView.Length(selfLease!.Ptr);
            }
            finally
            {
                selfLease?.Release();
            }
        }
    }

    public byte Get(uint index)
    {
        unsafe
        {
            BorrowLease<Raw.OpaqueSliceView>? selfLease = null;
            try
            {
                selfLease = Handle.Lease(BorrowKind.Shared);
                return Raw.OpaqueSliceView.Get(selfLease!.Ptr, index);
            }
            finally
            {
                selfLease?.Release();
            }
        }
    }

    public uint Sum()
    {
        unsafe
        {
            BorrowLease<Raw.OpaqueSliceView>? selfLease = null;
            try
            {
                selfLease = Handle.Lease(BorrowKind.Shared);
                return Raw.OpaqueSliceView.Sum(selfLease!.Ptr);
            }
            finally
            {
                selfLease?.Release();
            }
        }
    }
    /// <summary>
    /// Releases this wrapper's native resource immediately.
    /// </summary>
    /// <remarks>
    /// Retained-borrow returns sourced from a manually_disposable opaque are
    /// rejected during generation, so Dispose is not a parent-invalidation API.
    /// Callers must not race Dispose with calls from another thread. Later calls
    /// throw <see cref="ObjectDisposedException"/>.
    /// </remarks>
    public void Dispose()
    {
        unsafe
        {
            RustHandle<Raw.OpaqueSliceView>? inner = _inner;
            if (inner is null)
            {
                return;
            }

            inner.ReleaseWrapper();
            _inner = null;
        }
    }
}