// <auto-generated/> by Diplomat

using System;
using Somelib.Diplomat;

namespace Somelib;

#nullable enable

public partial class One
{
    private unsafe RustHandle<Raw.One>? _inner;

    private static readonly unsafe RustDestructor<Raw.One> _destroy = Raw.One.Destroy;

    /// <summary>
    /// Creates a managed <c>One</c> from a raw handle.
    /// </summary>
    /// <remarks>
    /// Safety: you should not build two managed objects using the same raw handle (may cause use-after-free and double-free).
    /// <br/>
    /// This constructor assumes the raw struct is allocated on Rust side.
    /// If implemented, the custom Drop implementation on Rust side WILL run on destruction.
    /// </remarks>
    internal unsafe One(Raw.One* handle)
    {
        _inner = RustHandle<Raw.One>.Owned(handle, _destroy);
    }

    /// <summary>
    /// Owned construction with lifetime edges released after the Rust
    /// destructor.
    /// </summary>
    internal unsafe One(Raw.One* handle, params ILifetimeEdge?[] edges)
    {
        _inner = RustHandle<Raw.One>.Owned(handle, _destroy, edges);
    }

    internal unsafe One(
        Raw.One* handle,
        WrapperKind kind,
        params ILifetimeEdge?[] edges)
    {
        _inner = RustHandle<Raw.One>.Borrowed(handle, kind, edges);
    }

    internal unsafe RustHandle<Raw.One> _diplomatHandle
    {
        get
        {
            RustHandle<Raw.One>? inner = _inner;
            if (inner is null || inner.IsClosed)
            {
                throw new ObjectDisposedException("One");
            }
            return inner;
        }
    }

    /// <returns>
    /// A <c>One</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// </remarks>
    public static One Transitivity(One hold, One nohold)
    {
        unsafe
        {
            if (hold == null) throw new ArgumentNullException(nameof(hold));
            if (nohold == null) throw new ArgumentNullException(nameof(nohold));
            BorrowLease<Raw.One>? holdLease = null;
            BorrowLease<Raw.One>? noholdLease = null;
            try
            {
                holdLease = hold._diplomatHandle.Lease(BorrowKind.Shared);
                noholdLease = nohold._diplomatHandle.Lease(BorrowKind.Shared);
                Raw.One* result = Raw.One.Transitivity(holdLease!.Ptr, noholdLease!.Ptr);
                return new One(result, LifetimeEdge.Move(ref holdLease));
            }
            finally
            {
                holdLease?.Release();
                noholdLease?.Release();
            }
        }
    }

    /// <returns>
    /// A <c>One</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// </remarks>
    public static One Cycle(Two hold, One nohold)
    {
        unsafe
        {
            if (hold == null) throw new ArgumentNullException(nameof(hold));
            if (nohold == null) throw new ArgumentNullException(nameof(nohold));
            BorrowLease<Raw.Two>? holdLease = null;
            BorrowLease<Raw.One>? noholdLease = null;
            try
            {
                holdLease = hold._diplomatHandle.Lease(BorrowKind.Shared);
                noholdLease = nohold._diplomatHandle.Lease(BorrowKind.Shared);
                Raw.One* result = Raw.One.Cycle(holdLease!.Ptr, noholdLease!.Ptr);
                return new One(result, LifetimeEdge.Move(ref holdLease));
            }
            finally
            {
                holdLease?.Release();
                noholdLease?.Release();
            }
        }
    }

    /// <returns>
    /// A <c>One</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// </remarks>
    public static One ManyDependents(One a, One b, Two c, Two d, Two nohold)
    {
        unsafe
        {
            if (a == null) throw new ArgumentNullException(nameof(a));
            if (b == null) throw new ArgumentNullException(nameof(b));
            if (c == null) throw new ArgumentNullException(nameof(c));
            if (d == null) throw new ArgumentNullException(nameof(d));
            if (nohold == null) throw new ArgumentNullException(nameof(nohold));
            BorrowLease<Raw.One>? aLease = null;
            BorrowLease<Raw.One>? bLease = null;
            BorrowLease<Raw.Two>? cLease = null;
            BorrowLease<Raw.Two>? dLease = null;
            BorrowLease<Raw.Two>? noholdLease = null;
            try
            {
                aLease = a._diplomatHandle.Lease(BorrowKind.Shared);
                bLease = b._diplomatHandle.Lease(BorrowKind.Shared);
                cLease = c._diplomatHandle.Lease(BorrowKind.Shared);
                dLease = d._diplomatHandle.Lease(BorrowKind.Shared);
                noholdLease = nohold._diplomatHandle.Lease(BorrowKind.Shared);
                Raw.One* result = Raw.One.ManyDependents(aLease!.Ptr, bLease!.Ptr, cLease!.Ptr, dLease!.Ptr, noholdLease!.Ptr);
                return new One(result, LifetimeEdge.Move(ref aLease), LifetimeEdge.Move(ref bLease), LifetimeEdge.Move(ref cLease), LifetimeEdge.Move(ref dLease));
            }
            finally
            {
                aLease?.Release();
                bLease?.Release();
                cLease?.Release();
                dLease?.Release();
                noholdLease?.Release();
            }
        }
    }

    /// <returns>
    /// A <c>One</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// </remarks>
    public static One ReturnOutlivesParam(Two hold, One nohold)
    {
        unsafe
        {
            if (hold == null) throw new ArgumentNullException(nameof(hold));
            if (nohold == null) throw new ArgumentNullException(nameof(nohold));
            BorrowLease<Raw.Two>? holdLease = null;
            BorrowLease<Raw.One>? noholdLease = null;
            try
            {
                holdLease = hold._diplomatHandle.Lease(BorrowKind.Shared);
                noholdLease = nohold._diplomatHandle.Lease(BorrowKind.Shared);
                Raw.One* result = Raw.One.ReturnOutlivesParam(holdLease!.Ptr, noholdLease!.Ptr);
                return new One(result, LifetimeEdge.Move(ref holdLease));
            }
            finally
            {
                holdLease?.Release();
                noholdLease?.Release();
            }
        }
    }

    /// <returns>
    /// A <c>One</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// </remarks>
    public static One DiamondTop(One top, One left, One right, One bottom)
    {
        unsafe
        {
            if (top == null) throw new ArgumentNullException(nameof(top));
            if (left == null) throw new ArgumentNullException(nameof(left));
            if (right == null) throw new ArgumentNullException(nameof(right));
            if (bottom == null) throw new ArgumentNullException(nameof(bottom));
            BorrowLease<Raw.One>? topLease = null;
            BorrowLease<Raw.One>? leftLease = null;
            BorrowLease<Raw.One>? rightLease = null;
            BorrowLease<Raw.One>? bottomLease = null;
            try
            {
                topLease = top._diplomatHandle.Lease(BorrowKind.Shared);
                leftLease = left._diplomatHandle.Lease(BorrowKind.Shared);
                rightLease = right._diplomatHandle.Lease(BorrowKind.Shared);
                bottomLease = bottom._diplomatHandle.Lease(BorrowKind.Shared);
                Raw.One* result = Raw.One.DiamondTop(topLease!.Ptr, leftLease!.Ptr, rightLease!.Ptr, bottomLease!.Ptr);
                return new One(result, LifetimeEdge.Move(ref topLease), LifetimeEdge.Move(ref leftLease), LifetimeEdge.Move(ref rightLease), LifetimeEdge.Move(ref bottomLease));
            }
            finally
            {
                topLease?.Release();
                leftLease?.Release();
                rightLease?.Release();
                bottomLease?.Release();
            }
        }
    }

    /// <returns>
    /// A <c>One</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// </remarks>
    public static One DiamondLeft(One top, One left, One right, One bottom)
    {
        unsafe
        {
            if (top == null) throw new ArgumentNullException(nameof(top));
            if (left == null) throw new ArgumentNullException(nameof(left));
            if (right == null) throw new ArgumentNullException(nameof(right));
            if (bottom == null) throw new ArgumentNullException(nameof(bottom));
            BorrowLease<Raw.One>? topLease = null;
            BorrowLease<Raw.One>? leftLease = null;
            BorrowLease<Raw.One>? rightLease = null;
            BorrowLease<Raw.One>? bottomLease = null;
            try
            {
                topLease = top._diplomatHandle.Lease(BorrowKind.Shared);
                leftLease = left._diplomatHandle.Lease(BorrowKind.Shared);
                rightLease = right._diplomatHandle.Lease(BorrowKind.Shared);
                bottomLease = bottom._diplomatHandle.Lease(BorrowKind.Shared);
                Raw.One* result = Raw.One.DiamondLeft(topLease!.Ptr, leftLease!.Ptr, rightLease!.Ptr, bottomLease!.Ptr);
                return new One(result, LifetimeEdge.Move(ref leftLease), LifetimeEdge.Move(ref bottomLease));
            }
            finally
            {
                topLease?.Release();
                leftLease?.Release();
                rightLease?.Release();
                bottomLease?.Release();
            }
        }
    }

    /// <returns>
    /// A <c>One</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// </remarks>
    public static One DiamondRight(One top, One left, One right, One bottom)
    {
        unsafe
        {
            if (top == null) throw new ArgumentNullException(nameof(top));
            if (left == null) throw new ArgumentNullException(nameof(left));
            if (right == null) throw new ArgumentNullException(nameof(right));
            if (bottom == null) throw new ArgumentNullException(nameof(bottom));
            BorrowLease<Raw.One>? topLease = null;
            BorrowLease<Raw.One>? leftLease = null;
            BorrowLease<Raw.One>? rightLease = null;
            BorrowLease<Raw.One>? bottomLease = null;
            try
            {
                topLease = top._diplomatHandle.Lease(BorrowKind.Shared);
                leftLease = left._diplomatHandle.Lease(BorrowKind.Shared);
                rightLease = right._diplomatHandle.Lease(BorrowKind.Shared);
                bottomLease = bottom._diplomatHandle.Lease(BorrowKind.Shared);
                Raw.One* result = Raw.One.DiamondRight(topLease!.Ptr, leftLease!.Ptr, rightLease!.Ptr, bottomLease!.Ptr);
                return new One(result, LifetimeEdge.Move(ref rightLease), LifetimeEdge.Move(ref bottomLease));
            }
            finally
            {
                topLease?.Release();
                leftLease?.Release();
                rightLease?.Release();
                bottomLease?.Release();
            }
        }
    }

    /// <returns>
    /// A <c>One</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// </remarks>
    public static One DiamondBottom(One top, One left, One right, One bottom)
    {
        unsafe
        {
            if (top == null) throw new ArgumentNullException(nameof(top));
            if (left == null) throw new ArgumentNullException(nameof(left));
            if (right == null) throw new ArgumentNullException(nameof(right));
            if (bottom == null) throw new ArgumentNullException(nameof(bottom));
            BorrowLease<Raw.One>? topLease = null;
            BorrowLease<Raw.One>? leftLease = null;
            BorrowLease<Raw.One>? rightLease = null;
            BorrowLease<Raw.One>? bottomLease = null;
            try
            {
                topLease = top._diplomatHandle.Lease(BorrowKind.Shared);
                leftLease = left._diplomatHandle.Lease(BorrowKind.Shared);
                rightLease = right._diplomatHandle.Lease(BorrowKind.Shared);
                bottomLease = bottom._diplomatHandle.Lease(BorrowKind.Shared);
                Raw.One* result = Raw.One.DiamondBottom(topLease!.Ptr, leftLease!.Ptr, rightLease!.Ptr, bottomLease!.Ptr);
                return new One(result, LifetimeEdge.Move(ref bottomLease));
            }
            finally
            {
                topLease?.Release();
                leftLease?.Release();
                rightLease?.Release();
                bottomLease?.Release();
            }
        }
    }

    /// <returns>
    /// A <c>One</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// </remarks>
    public static One DiamondAndNestedTypes(One a, One b, One c, One d, One nohold)
    {
        unsafe
        {
            if (a == null) throw new ArgumentNullException(nameof(a));
            if (b == null) throw new ArgumentNullException(nameof(b));
            if (c == null) throw new ArgumentNullException(nameof(c));
            if (d == null) throw new ArgumentNullException(nameof(d));
            if (nohold == null) throw new ArgumentNullException(nameof(nohold));
            BorrowLease<Raw.One>? aLease = null;
            BorrowLease<Raw.One>? bLease = null;
            BorrowLease<Raw.One>? cLease = null;
            BorrowLease<Raw.One>? dLease = null;
            BorrowLease<Raw.One>? noholdLease = null;
            try
            {
                aLease = a._diplomatHandle.Lease(BorrowKind.Shared);
                bLease = b._diplomatHandle.Lease(BorrowKind.Shared);
                cLease = c._diplomatHandle.Lease(BorrowKind.Shared);
                dLease = d._diplomatHandle.Lease(BorrowKind.Shared);
                noholdLease = nohold._diplomatHandle.Lease(BorrowKind.Shared);
                Raw.One* result = Raw.One.DiamondAndNestedTypes(aLease!.Ptr, bLease!.Ptr, cLease!.Ptr, dLease!.Ptr, noholdLease!.Ptr);
                return new One(result, LifetimeEdge.Move(ref aLease), LifetimeEdge.Move(ref bLease), LifetimeEdge.Move(ref cLease), LifetimeEdge.Move(ref dLease));
            }
            finally
            {
                aLease?.Release();
                bLease?.Release();
                cLease?.Release();
                dLease?.Release();
                noholdLease?.Release();
            }
        }
    }

    /// <returns>
    /// A <c>One</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// </remarks>
    public static One ImplicitBounds(One explicitHold, One implicitHold, One nohold)
    {
        unsafe
        {
            if (explicitHold == null) throw new ArgumentNullException(nameof(explicitHold));
            if (implicitHold == null) throw new ArgumentNullException(nameof(implicitHold));
            if (nohold == null) throw new ArgumentNullException(nameof(nohold));
            BorrowLease<Raw.One>? explicitHoldLease = null;
            BorrowLease<Raw.One>? implicitHoldLease = null;
            BorrowLease<Raw.One>? noholdLease = null;
            try
            {
                explicitHoldLease = explicitHold._diplomatHandle.Lease(BorrowKind.Shared);
                implicitHoldLease = implicitHold._diplomatHandle.Lease(BorrowKind.Shared);
                noholdLease = nohold._diplomatHandle.Lease(BorrowKind.Shared);
                Raw.One* result = Raw.One.ImplicitBounds(explicitHoldLease!.Ptr, implicitHoldLease!.Ptr, noholdLease!.Ptr);
                return new One(result, LifetimeEdge.Move(ref explicitHoldLease), LifetimeEdge.Move(ref implicitHoldLease));
            }
            finally
            {
                explicitHoldLease?.Release();
                implicitHoldLease?.Release();
                noholdLease?.Release();
            }
        }
    }

    /// <returns>
    /// A <c>One</c> allocated on Rust side.
    /// </returns>
    /// <remarks>
    /// Lifetime: the returned native-backed value may borrow from the receiver or one or more inputs.
    /// Source handles stay reachable while the returned value exists.
    /// </remarks>
    public static One ImplicitBoundsDeep(One @explicit, One implicit1, One implicit2, One nohold)
    {
        unsafe
        {
            if (@explicit == null) throw new ArgumentNullException(nameof(@explicit));
            if (implicit1 == null) throw new ArgumentNullException(nameof(implicit1));
            if (implicit2 == null) throw new ArgumentNullException(nameof(implicit2));
            if (nohold == null) throw new ArgumentNullException(nameof(nohold));
            BorrowLease<Raw.One>? explicitLease = null;
            BorrowLease<Raw.One>? implicit1lease = null;
            BorrowLease<Raw.One>? implicit2lease = null;
            BorrowLease<Raw.One>? noholdLease = null;
            try
            {
                explicitLease = @explicit._diplomatHandle.Lease(BorrowKind.Shared);
                implicit1lease = implicit1._diplomatHandle.Lease(BorrowKind.Shared);
                implicit2lease = implicit2._diplomatHandle.Lease(BorrowKind.Shared);
                noholdLease = nohold._diplomatHandle.Lease(BorrowKind.Shared);
                Raw.One* result = Raw.One.ImplicitBoundsDeep(explicitLease!.Ptr, implicit1lease!.Ptr, implicit2lease!.Ptr, noholdLease!.Ptr);
                return new One(result, LifetimeEdge.Move(ref explicitLease), LifetimeEdge.Move(ref implicit1lease), LifetimeEdge.Move(ref implicit2lease));
            }
            finally
            {
                explicitLease?.Release();
                implicit1lease?.Release();
                implicit2lease?.Release();
                noholdLease?.Release();
            }
        }
    }

    private void Cleanup()
    {
        unsafe
        {
            RustHandle<Raw.One>? inner = _inner;
            if (inner is null)
            {
                return;
            }

            inner.ReleaseWrapper();
            _inner = null;
        }
    }
}