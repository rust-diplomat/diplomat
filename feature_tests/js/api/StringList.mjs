// generated by diplomat-tool
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const StringList_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.StringList_destroy(ptr);
});

export class StringList {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];
    
    constructor(ptr, selfEdge) {
        
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;
        // Unconditionally register to destroy when this object is ready to garbage collect.
        StringList_box_destroy_registry.register(this, this.#ptr);
    }

    get ffiValue() {
        return this.#ptr;
    }

    static new_(v) {
        
        const vSlice = diplomatRuntime.DiplomatBuf.str8(wasm, v);
        const result = wasm.StringList_new(vSlice.ptr, vSlice.size);
    
        try {
            return new StringList(result, []);
        }
        
        finally {
            vSlice.free();
        }
    }

    get str() {
        
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);
        wasm.StringList_get_str(this.ffiValue, write.buffer);
    
        try {
            return write.readString8();
        }
        
        finally {
            write.free();
        }
    }
}