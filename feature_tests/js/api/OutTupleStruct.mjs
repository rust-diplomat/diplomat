// generated by diplomat-tool
import { Opaque } from "./Opaque.mjs"
import { PrimitiveStruct } from "./PrimitiveStruct.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



export class OutTupleStruct {
    #x;
    get x() {
        return this.#x;
    }
    #y;
    get y() {
        return this.#y;
    }
    #primitive;
    get primitive() {
        return this.#primitive;
    }
    #opaque;
    get opaque() {
        return this.#opaque;
    }
    #internalConstructor(structObj, internalConstructor) {
        if (typeof structObj !== "object") {
            throw new Error("OutTupleStruct's constructor takes an object of OutTupleStruct's fields.");
        }

        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("OutTupleStruct is an out struct and can only be created internally.");
        }
        if ("x" in structObj) {
            this.#x = structObj.x;
        } else {
            throw new Error("Missing required field x.");
        }

        if ("y" in structObj) {
            this.#y = structObj.y;
        } else {
            throw new Error("Missing required field y.");
        }

        if ("primitive" in structObj) {
            this.#primitive = PrimitiveStruct._fromSuppliedValue(diplomatRuntime.internalConstructor, structObj.primitive);
        } else {
            throw new Error("Missing required field primitive.");
        }

        if ("opaque" in structObj) {
            this.#opaque = structObj.opaque;
        } else {
            throw new Error("Missing required field opaque.");
        }

        return this;
    }

    // Return this struct in FFI function friendly format.
    // Returns an array that can be expanded with spread syntax (...)
    _intoFFI(
        dst,
        functionCleanupArena,
        appendArrayMap
    ) {

        this._writeToArrayBuffer(wasm.memory.buffer, dst, functionCleanupArena, appendArrayMap);

        return dst;
    }

    static get _sizeBytes() {
        return 48;
    }

    /// Currently unused, we may want to use later on though:
    static get _sizeAlign() {
        return 8;
    }

    static _fromSuppliedValue(internalConstructor, obj) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("_fromSuppliedValue cannot be called externally.");
        }

        if (obj instanceof OutTupleStruct) {
            return obj;
        }

        return OutTupleStruct.fromFields(obj);
    }

    _writeToArrayBuffer(
        arrayBuffer,
        offset,
        functionCleanupArena,
        appendArrayMap
    ) {
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, this.#x, Int32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 4, this.#y, Int32Array);
        PrimitiveStruct._fromSuppliedValue(diplomatRuntime.internalConstructor, this.#primitive)._writeToArrayBuffer(arrayBuffer, offset + 8, functionCleanupArena, {});
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 40, this.#opaque.ffiValue, Uint32Array);
    }

    // This struct contains borrowed fields, so this takes in a list of
    // "edges" corresponding to where each lifetime's data may have been borrowed from
    // and passes it down to individual fields containing the borrow.
    // This method does not attempt to handle any dependencies between lifetimes, the caller
    // should handle this when constructing edge arrays.
    static _fromFFI(internalConstructor, ptr) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("OutTupleStruct._fromFFI is not meant to be called externally. Please use the default constructor.");
        }
        let structObj = {};
        const xDeref = (new Int32Array(wasm.memory.buffer, ptr, 1))[0];
        structObj.x = xDeref;
        const yDeref = (new Int32Array(wasm.memory.buffer, ptr + 4, 1))[0];
        structObj.y = yDeref;
        const primitiveDeref = ptr + 8;
        structObj.primitive = PrimitiveStruct._fromFFI(diplomatRuntime.internalConstructor, primitiveDeref);
        const opaqueDeref = diplomatRuntime.ptrRead(wasm, ptr + 40);
        structObj.opaque = new Opaque(diplomatRuntime.internalConstructor, opaqueDeref, []);

        return new OutTupleStruct(structObj, internalConstructor);
    }


    static new_() {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 48, 8, false);


        const result = wasm.OutTupleStruct_new(diplomatReceive.buffer);

        try {
            return OutTupleStruct._fromFFI(diplomatRuntime.internalConstructor, diplomatReceive.buffer);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    constructor(structObj, internalConstructor) {
        return this.#internalConstructor(...arguments)
    }
}