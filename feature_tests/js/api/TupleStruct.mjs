// generated by diplomat-tool
import { ContainingTuple } from "./ContainingTuple.mjs"
import { MyStruct } from "./MyStruct.mjs"
import { Opaque } from "./Opaque.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



export class TupleStruct {
    #x;
    get x() {
        return this.#x;
    }
    set x(value){
        this.#x = value;
    }
    #y;
    get y() {
        return this.#y;
    }
    set y(value){
        this.#y = value;
    }
    #st;
    get st() {
        return this.#st;
    }
    set st(value){
        this.#st = value;
    }
    #op;
    get op() {
        return this.#op;
    }
    set op(value){
        this.#op = value;
    }
    /** @internal */
    static fromFields(structObj) {
        return new TupleStruct(structObj);
    }

    #internalConstructor(structObj) {
        if (typeof structObj !== "object") {
            throw new Error("TupleStruct's constructor takes an object of TupleStruct's fields.");
        }

        if ("x" in structObj) {
            this.#x = structObj.x;
        } else {
            throw new Error("Missing required field x.");
        }

        if ("y" in structObj) {
            this.#y = structObj.y;
        } else {
            throw new Error("Missing required field y.");
        }

        if ("st" in structObj) {
            this.#st = MyStruct._fromSuppliedValue(diplomatRuntime.internalConstructor, structObj.st);
        } else {
            throw new Error("Missing required field st.");
        }

        if ("op" in structObj) {
            this.#op = structObj.op;
        } else {
            throw new Error("Missing required field op.");
        }

        return this;
    }

    // Return this struct in FFI function friendly format.
    // Returns an array that can be expanded with spread syntax (...)// If this struct contains any slices, their lifetime-edge-relevant information will be
    // set up here, and can be appended to any relevant lifetime arrays here. <lifetime>AppendArray accepts a list
    // of arrays for each lifetime to do so. It accepts multiple lists per lifetime in case the caller needs to tie a lifetime to multiple
    // output arrays. Null is equivalent to an empty list: this lifetime is not being borrowed from.
    _intoFFI(
        dst,
        functionCleanupArena,
        appendArrayMap
    ) {

        this._writeToArrayBuffer(wasm.memory.buffer, dst, functionCleanupArena, appendArrayMap);

        return dst;
    }

    static get _sizeBytes() {
        return 48;
    }

    /// Currently unused, we may want to use later on though:
    static get _sizeAlign() {
        return 8;
    }

    static _fromSuppliedValue(internalConstructor, obj) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("_fromSuppliedValue cannot be called externally.");
        }

        if (obj instanceof TupleStruct) {
            return obj;
        }

        return TupleStruct.fromFields(obj);
    }

    _writeToArrayBuffer(
        arrayBuffer,
        offset,
        functionCleanupArena,
        appendArrayMap
    ) {
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, this.#x, Int32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 4, this.#y, Int32Array);
        MyStruct._fromSuppliedValue(diplomatRuntime.internalConstructor, this.#st)._writeToArrayBuffer(arrayBuffer, offset + 8, functionCleanupArena, {});
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 40, this.#op.ffiValue, Uint32Array);
    }

    static _fromFFI(internalConstructor, ptr, aEdges) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("TupleStruct._fromFFI is not meant to be called externally. Please use the default constructor.");
        }
        let structObj = {};
        const xDeref = (new Int32Array(wasm.memory.buffer, ptr, 1))[0];
        structObj.x = xDeref;
        const yDeref = (new Int32Array(wasm.memory.buffer, ptr + 4, 1))[0];
        structObj.y = yDeref;
        const stDeref = ptr + 8;
        structObj.st = MyStruct._fromFFI(diplomatRuntime.internalConstructor, stDeref);
        const opDeref = diplomatRuntime.ptrRead(wasm, ptr + 40);
        structObj.op = new Opaque(diplomatRuntime.internalConstructor, opDeref, aEdges);

        return new TupleStruct(structObj);
    }

    // Return all fields corresponding to lifetime `'a`
    // without handling lifetime dependencies (this is the job of the caller)
    // This is all fields that may be borrowed from if borrowing `'a`,
    // assuming that there are no `'other: a`. bounds. In case of such bounds,
    // the caller should take care to also call _fieldsForLifetimeOther
    get _fieldsForLifetimeA() {
        return [this.#op];
    };


    static takesStAsTuple(a) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();


        const result = wasm.TupleStruct_takes_st_as_tuple(TupleStruct._fromSuppliedValue(diplomatRuntime.internalConstructor, a)._intoFFI(diplomatRuntime.FUNCTION_PARAM_ALLOC.alloc(TupleStruct._sizeBytes), functionCleanupArena, {}, false));

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

        }
    }

    static takesContaining(c) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();


        const result = wasm.TupleStruct_takes_containing(ContainingTuple._fromSuppliedValue(diplomatRuntime.internalConstructor, c)._intoFFI(diplomatRuntime.FUNCTION_PARAM_ALLOC.alloc(ContainingTuple._sizeBytes), functionCleanupArena, {}, false));

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

        }
    }

    constructor(structObj) {
        return this.#internalConstructor(...arguments)
    }
}