// generated by diplomat-tool
import { ResultOpaque } from "./ResultOpaque.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



export class FallibleOpaqueConstructor {
    #x;
    get x() {
        return this.#x;
    }
    set x(value){
        this.#x = value;
    }
    /** @internal */
    static fromFields(structObj) {
        return new FallibleOpaqueConstructor(diplomatRuntime.exposeConstructor, structObj);
    }

    #internalConstructor(structObj) {
        if (typeof structObj !== "object") {
            throw new Error("FallibleOpaqueConstructor's constructor takes an object of FallibleOpaqueConstructor's fields.");
        }

        if ("x" in structObj) {
            this.#x = structObj.x;
        } else {
            throw new Error("Missing required field x.");
        }

        return this;
    }

    // Return this struct in FFI function friendly format.
    // Returns an array that can be expanded with spread syntax (...)
    _intoFFI(
        dst,
        functionCleanupArena,
        appendArrayMap
    ) {
        return this.#x;
    }

    static get _sizeBytes() {
        return 4;
    }

    /// Currently unused, we may want to use later on though:
    static get _sizeAlign() {
        return 4;
    }

    static _fromSuppliedValue(internalConstructor, obj) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("_fromSuppliedValue cannot be called externally.");
        }

        if (obj instanceof FallibleOpaqueConstructor) {
            return obj;
        }

        return FallibleOpaqueConstructor.fromFields(obj);
    }

    _writeToArrayBuffer(
        arrayBuffer,
        offset,
        functionCleanupArena,
        appendArrayMap
    ) {
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, this.#x, Int32Array);
    }

    // This struct contains borrowed fields, so this takes in a list of
    // "edges" corresponding to where each lifetime's data may have been borrowed from
    // and passes it down to individual fields containing the borrow.
    // This method does not attempt to handle any dependencies between lifetimes, the caller
    // should handle this when constructing edge arrays.
    static _fromFFI(internalConstructor, primitiveValue) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("FallibleOpaqueConstructor._fromFFI is not meant to be called externally. Please use the default constructor.");
        }
        let structObj = {};
        structObj.x = primitiveValue;

        return new FallibleOpaqueConstructor(diplomatRuntime.exposeConstructor, structObj);
    }


    #defaultConstructor() {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.FallibleOpaqueConstructor_ctor(diplomatReceive.buffer);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new ResultOpaque(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
                throw new globalThis.Error('ResultOpaque: ' + cause.toString(), { cause });
            }
            return FallibleOpaqueConstructor._fromFFI(diplomatRuntime.internalConstructor, (new Int32Array(wasm.memory.buffer, diplomatReceive.buffer, 1))[0]);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    constructor() {
        if (arguments[0] === diplomatRuntime.exposeConstructor) {
            return this.#internalConstructor(...Array.prototype.slice.call(arguments, 1));
        } else if (arguments[0] === diplomatRuntime.internalConstructor) {
            return this.#internalConstructor(...arguments);
        } else {
            return this.#defaultConstructor(...arguments);
        }
    }
}